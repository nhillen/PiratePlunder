# Production environment configuration
# Backend is served on the same origin, so leave VITE_BACKEND_URL unset
# This allows getBackendUrl() to return empty string for same-origin requests
#
# DO NOT set VITE_BACKEND_URL here - it will cause CORS issues